apiVersion: v1
kind: Service
metadata:
  name: agents
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agents
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

