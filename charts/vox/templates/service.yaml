apiVersion: v1
kind: Service
metadata:
  name: vox
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vox
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

